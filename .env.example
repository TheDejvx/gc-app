# MongoDB Atlas connection string (use gc_app database)
MONGODB_URI=mongodb+srv://<user>:<password>@<cluster>.mongodb.net/gc_app?retryWrites=true&w=majority

# Server port (Railway sets this automatically)
PORT=5001
